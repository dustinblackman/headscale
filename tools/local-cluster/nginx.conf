events {
  worker_connections  1024;
}

http {
  server {
    listen 0.0.0.0:80;

    location / {
      access_log off;
      add_header 'Content-Type' 'text/plain';
      return 200 "healthy\n";
    }
  }
}
